[build-system]
requires = ["meson-python", "meson", "ninja", "pybind11"]
build-backend = "mesonpy"

[project]
name = "alphazero"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["zstandard>=0.19.0"]

[project.optional-dependencies]
dev = ["ninja", "meson", "pybind11"]
